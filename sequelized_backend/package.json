{
  "name": "bikersby",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:local": "cross-env NODE_ENV=development_local node index.js",
    "start:remote": "cross-env NODE_ENV=development_remote node index.js",
    "dev:local": "cross-env NODE_ENV=development_local nodemon index.js",
    "dev:remote": "cross-env NODE_ENV=development_remote nodemon index.js",
    "migrate:local": "cross-env NODE_ENV=development_local sequelize-cli db:migrate",
    "migrate:remote": "cross-env NODE_ENV=development_remote sequelize-cli db:migrate"
  },
  "author": "Hemlal & Sunil",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "sequelize": "^6.35.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.0.1",
    "sequelize-cli": "^6.6.2"
  }
}
